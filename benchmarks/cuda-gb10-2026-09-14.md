# Current NVIDIA GB10 CUDA proof — 2026-09-14

Commit `b878702e575e43eb1a5a9765a860133d25f8082b` was fetched from
`origin/main` into a fresh isolated checkout and rebuilt on
`promaxgb10-d666` (Linux aarch64, NVIDIA GB10, driver 580.173.02). Audio entered
through the file benchmark path; no microphone or speakers were used. The
compact output is retained in the
[current evidence directory](cuda-gb10-current-results-2026-09-14/).

## Current artifact and native contract

- Omawake executable: `e5170ce6fc50f614311160db6747bd60daaf09c8e389ef23ecacc2fc8542dfb2`
- sherpa-onnx upstream: `11afbd009a7f8c08f4bcf2fc1b265d0df4670fbf`
- extended-runtime patch: `b462ad2f88bbd5811df8180d40e4bd798569b0a1aa58fd9aaa4a38394e293f86`
- KWS exception-safety patch: `ad895ce231ec7ce2d1e9575450b6493c88d2272b8d3bbded8ad4a24a26d8944a`
- patched Sherpa DSO: `3a4ac5811cad403991a5a68a5455d57789af0dd8b0a2d8a0baa7e5d0b80886e1`
- ONNX Runtime 1.29 core: `cbeb3c5cf2de5a47f986c12b1e8901404880ab6abb27299ffdef2040125f984f`
- CUDA provider: `d7aaeea019ba6d4e3a9c03f0a6a863bf39c87d4c79a6d31466c646abe2b40f07`
- CUDA 13.0.3 and cuDNN 9.26.0

The Sherpa DSO has an exact `$ORIGIN` RUNPATH, exports
`SherpaOnnxGetExtendedApiVersion`, and does not export the retired
product-specific marker. Its CUDA provider-mode native contract passed
registration, idempotence, conflicting-path rejection, concurrent error
isolation, spotter-retained provider lifetime, post-teardown re-registration,
and exception-safe KWS creation without a retained runtime.

## Current setup and detection parity

The inventory returned all eight runtime/device rows. Default CPU auto/CPU and
CUDA auto/GPU were ready; the four OpenVINO rows correctly reported unavailable
on this NVIDIA host. CUDA preview reported `applied=false`, registered the
provider, selected the GPU, and preserved the config SHA exactly. Explicit
apply reported `applied=true` and persisted the fresh Sherpa and external
ORT/CUDA paths. No service was installed.

The cold process detected the exact three expected IDs: `light-up` from
`0.wav`, then `lovely-child` and `forever` from `1.wav`. The hot process found
the same IDs in every measured iteration, for 15/15 detections without misses
or additions.

| Phase | Load | `0.wav` | `1.wav` |
| --- | ---: | ---: | ---: |
| cold | 594.58 ms | 584.84 ms, RTF 0.08828 | 480.65 ms, RTF 0.02876 |
| hot | reused process | p50 186.95 ms, p95 187.60 ms | p50 463.40 ms, p95 464.38 ms |

`nvidia-smi pmon` observed the exact Omawake process at 314 MiB and up to 50%
SM. Nsight captured 77 CUDA kernel rows and 157,774 launches, including 57 ONNX
Runtime CUDA rows plus cuDNN, cuBLAS, and CUTLASS work. SGLang's PID set and GPU
allocation were unchanged before and after; its scheduler remained at 78,081
MiB.

## Earlier superseded measurements

The measurements below used the previous native adapter. They remain as
historical performance context and do not identify the current patch artifact.

The current runtime-neutral Omawake executable was validated on
`promaxgb10-d666` (Linux aarch64, NVIDIA GB10, driver 580.173.02). The external
stack used ONNX Runtime 1.29.0, its CUDA provider, CUDA 13.0 vendor libraries,
cuDNN 9.26, and the patched sherpa-onnx 1.13.8 adapter. Setup previewed and
probed those user-supplied libraries before an explicit apply. They are not
part of the default CPU release runtime.

The catalog's full-FP32 CUDA encoder, decoder, and joiner were used for the
like-for-like CPU comparison. Audio entered through the file benchmark path;
no microphone or speakers were used.

## Runtime, placement, and detection parity

The setup preview reported `applied=false`, then the explicit apply reported
`applied=true`. Both probes registered the CUDA provider and selected the GPU.
All CUDA runs reported `effective_runtime="cuda"` and `fallback_used=false`.

Nsight Systems captured 77 CUDA kernel summary rows from the Omawake process.
The leading convolution kernel ran 33,973 times; the trace also contains
ONNX Runtime CUDA elementwise/slice kernels, cuDNN convolution, cuBLAS, and
CUTLASS GEMM work. `nvidia-smi pmon` independently observed Omawake using up to
28% SM and 314 MiB of framebuffer memory.

Both CPU FP32 and CUDA produced the same detections on every iteration:

| Fixture | Expected and observed detections | CPU runs | CUDA runs |
| --- | --- | ---: | ---: |
| `test_wavs/0.wav` | `light-up` | 11/11 | 11/11 |
| `test_wavs/1.wav` | `lovely-child`, `forever` | 11/11 | 11/11 |

That is 22/22 file-level parity comparisons with no missed or added wake-word
detections.

## Cold and hot timing

Cold rows use a new process and one measured iteration per fixture. Hot rows
use a new process, two warmups, and ten measured iterations per fixture.

| Runtime | Load | Fixture | Cold | Hot p50 | Hot p95 | Hot p50 RTF |
| --- | ---: | --- | ---: | ---: | ---: | ---: |
| Default CPU, FP32 | 278.13 ms | `0.wav` | 73.60 ms | 69.82 ms | 70.06 ms | 0.01054 |
| Default CPU, FP32 | 278.13 ms | `1.wav` | 175.81 ms | 175.50 ms | 175.79 ms | 0.01050 |
| CUDA GPU, FP32 | 1,997.36 ms | `0.wav` | 2,086.07 ms | 2,461.60 ms | 2,985.16 ms | 0.37156 |
| CUDA GPU, FP32 | 1,997.36 ms | `1.wav` | 463.71 ms | 481.65 ms | 489.13 ms | 0.02882 |

The host was materially contended: an SGLang process occupied about 78 GiB of
GPU memory throughout the run. These timing figures demonstrate correct CUDA
operation under contention; they are not an isolated GB10 performance
baseline. The detection parity and process-specific Nsight trace remain valid
under that contention.

## Evidence

The tracked [evidence directory](cuda-gb10-results-2026-09-14/) contains:

- binary, adapter, patch, and runtime hashes in `identities.txt`;
- setup preview and apply probes;
- CPU INT8, CPU FP32, and CUDA FP32 cold/hot benchmark JSON;
- process telemetry and the Nsight CUDA kernel summary.

The validated executable SHA-256 is
`95e1914b185bb3bb839defdef8f0a8a17bf332d9e5d74a85cba8e955bfdb0959`.
