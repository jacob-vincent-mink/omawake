# Native provider benchmarks

Historical results from the removed prototype inference path were deleted.
New evidence must identify the exact provider build, model checksums, device,
placement proof, cold and hot timing, accuracy corpus, and idle duty/power.

The release gate and corpus method are defined in
[`docs/architecture/EVALUATION.md`](../docs/architecture/EVALUATION.md).
